/**
 * 解决环境报错： [vue-test-utils]: window is undefined, vue-test-utils needs to be run in a browser environment.
 * @jest-environment jsdom
 */

import Input from '../src/link.vue'
import { mount } from '@vue/test-utils'

describe('lg-Link', () => {

})
